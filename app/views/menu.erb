<section class="col-xs-12 col-sm-4">
  <h2>Menu</h2>
  <ul>
    <% @menu.each do |item| %>
      <li><a href="#" class="product"><%=  item[0] %></a></li>
      <li>Price: £<%= item[1] %></li>
    <% end %>
  </ul>
</section>